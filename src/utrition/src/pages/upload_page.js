import React from "react";

const Upload = () => {
  return (
    <div
      style={{
        color: "#187d04",
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        height: "90vh",
      }}
    >
      <h1>Upload</h1>
    </div>
  );
};

export default Upload;
